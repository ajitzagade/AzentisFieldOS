import { EmptyState, ClipboardIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<ClipboardIcon />} message="Daily Activity reports will appear here once Daily Site Reporting ships." />;
}
