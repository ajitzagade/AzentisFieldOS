import { EmptyState, BoxIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<BoxIcon />} message="Inventory stock levels will appear here once Inventory Lifecycle & Movement ships." />;
}
