import { EmptyState, BuildingIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<BuildingIcon />} message="Vendors will appear here once Vendor Management ships." />;
}
