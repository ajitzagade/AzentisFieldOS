import { EmptyState, DropletIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<DropletIcon />} message="RMC deliveries will appear here once RMC Tracking ships." />;
}
