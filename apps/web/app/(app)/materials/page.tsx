import { EmptyState, LayersIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<LayersIcon />} message="The material catalog will appear here once Material Catalog Configuration ships." />;
}
