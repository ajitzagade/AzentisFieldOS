import { EmptyState, TruckIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<TruckIcon />} message="Machinery and vehicle registers will appear here once Machinery & Vehicle Registers ships." />;
}
