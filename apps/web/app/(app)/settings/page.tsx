import { EmptyState, GearIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<GearIcon />} message="Branding, users, and category configuration will appear here once Admin Configuration ships." />;
}
