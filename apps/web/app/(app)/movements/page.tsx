import { EmptyState, ArrowsIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<ArrowsIcon />} message="Purchase, movement, and consumption records will appear here once Inventory Lifecycle & Movement ships." />;
}
