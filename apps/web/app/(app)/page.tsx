import { EmptyState, HomeIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<HomeIcon />} message="Today's Activity dashboard will appear here once Contractor Dashboard ships." />;
}
