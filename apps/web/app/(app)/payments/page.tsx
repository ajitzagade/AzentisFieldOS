import { EmptyState, WalletIcon } from "@azentisfieldos/ui";

export default function Page() {
  return <EmptyState icon={<WalletIcon />} message="Payments will appear here once Labour Advances & Payments ships." />;
}
